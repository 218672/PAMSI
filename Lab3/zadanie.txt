Porządek projektu:
1. Makefile
2. Foldery na nagłówki i źródłowe: inc, src, obj.

Porządek kodu:
1. Prywatność i dostęp do zmiennych poprzez inicjujące je metody (nie bezpośrednio).

ADT:
1. Ułatwienie pracy: jak skorzystać z danego typu danych
2. Zdefiniowane metody w interfejsie
3. Dokumentacja: określone założenia, ograniczenia dla metod, informacje o wyrzucanych wyjątkach (w Javie niepotrzebne), komunikacja błędów.

Stos, lista, kolejka:

Lista:

IList // niezbedne dla interfejsu listy
 add(Element, int); // co ma dodać i na jakiej pozycji
void remove(int); // usun na pozycji
element get(int); // zdejmij i zwroc
int size(); // rozmiar listy

IStack
push(element)
pop()
size()

lista, kolejka, przeszukiwanie listy



